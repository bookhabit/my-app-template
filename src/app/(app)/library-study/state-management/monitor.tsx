import React from 'react';
import { View, Text } from 'react-native';

// 전역상태라이브러리 5개 스토어에 저장된 값 분리해서 모두 화면 출력
// 스토어 불러오는 로직을 비교하기 위해
const MonitorScreen = () => {
  return (
    <View>
      <Text>monitor</Text>
    </View>
  );
};

export default MonitorScreen;
